# Production requirements (includes all base requirements + PostgreSQL)
-r requirements.txt

# Note: psycopg2-binary is already included in requirements.txt
# On Render/Railway, it will install automatically with pre-built wheels for Linux
